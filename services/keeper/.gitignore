node_modules/
dist/
.wrangler/
# Local secrets for `wrangler dev`. Never commit.
.dev.vars
.dev.vars.*
!.dev.vars.example
# First-deploy secrets for `wrangler deploy --secrets-file`. Never commit.
.secrets.*
.env
*.log
